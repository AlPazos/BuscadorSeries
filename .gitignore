# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# variables de entorno (no subir la API key)
.env
.dev.vars

# estado local de wrangler (Cloudflare)
.wrangler

# symlink local al contexto compartido con el backend (vive en ~/.claude,
# fuera del repo; lo enlazamos para que los agentes lo lean, no se commitea)

# Claude Code: settings locales (no commitear)
.claude/settings.local.json
boveda
opencode.json

# Agentes de IA (no commitear)
.opencode/
agents/
